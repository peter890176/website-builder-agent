# Generated Website Template

This internal Vite, React, TypeScript, and Tailwind CSS template is copied into each generated project before the LangGraph workflow writes the requested website files.

Keep this directory deterministic and free of secrets, dependencies, build output, and project-specific content.
